image:
  repository: ghcr.io/your-org/catalog-service
  tag: "0.0.1"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8081

env:
  DB_HOST: catalog-postgres
  DB_PORT: "5432"
  DB_NAME: catalog
  DB_USER: catalog
  DB_PASSWORD: catalog

resources: {}

replicaCount: 1


