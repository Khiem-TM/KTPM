image:
  repository: ghcr.io/your-org/borrowing-service
  tag: "0.0.1"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8083

env:
  DB_HOST: borrowing-postgres
  DB_PORT: "5432"
  DB_NAME: borrowing
  DB_USER: borrowing
  DB_PASSWORD: borrowing

replicaCount: 1
resources: {}
